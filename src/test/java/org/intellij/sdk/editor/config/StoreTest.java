package org.intellij.sdk.editor.config;

import junit.framework.TestCase;
import org.junit.Test;

import java.util.List;
import java.util.Map;

public class StoreTest extends TestCase {
/*
    @Test
    public void testGetRecords()
    {
        Store store = Store.getInstance();
        List<StoreRecord> records = store.getRecords();
        System.out.println(records);
        assertNotNull(records);
    }

    @Test
    public void testGetProviderList()
    {
        Store store = Store.getInstance();
        assertNull(store);
        Map<String, String> options = store.getProviderList();
        System.out.println(options);
        assertNotNull(options);
    }

    @Test
    public void testGetRecord()
    {
        Store store = Store.getInstance();
        StoreRecord rec = store.getRecord("NotExists");
        assertNull(rec);

        StoreRecord record = store.getRecord("OpenAI");
        System.out.println(record);
        assertNotNull(record);

        assertEquals("OpenAI", record.name);
        assertEquals("https://api.openai.com/v1", record.baseUrl);

        assertNotNull(record.models);
    }
    */

}